<p style="color: green"><%= notice %></p>


<div id="inspections" class="columns mt-6 is-centered is-multiline">
  <% @inspections.each do |inspection| %>
    <div class="column is-one-third">
      <%= render inspection %>
      <!-- <p>
      <%= link_to "شاهد الطلب", inspection %>
    </p> -->
    </div>
  <% end %>
</div>

